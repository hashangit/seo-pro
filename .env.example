# SEO Pro Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# WorkOS Authentication
# ============================================================================
# Get your credentials from https://dashboard.workos.com
# The dashboard provides: Client ID and API Key (NOT "Audience")
#
# Required for frontend (AuthKit React SDK) - use NEXT_PUBLIC_ prefix:
NEXT_PUBLIC_WORKOS_CLIENT_ID=client_xxxxxxxxxxxxxxxxxxxxxxxx
# For development, this would typically be http://localhost:3000
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000
#
# Required for backend (JWT validation - auto-set by config.py, override only if needed):
WORKOS_AUDIENCE=api.workos.com
WORKOS_ISSUER=api.workos.com
WORKOS_JWKS_URL=https://api.workos.com/v1/jwks
#
# Optional: For making WorkOS API calls from backend (user management, etc.):
# WORKOS_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# Supabase (Data Store Only - Auth handled by WorkOS)
# ============================================================================
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
# Secret key - Server-side only (bypasses RLS, never expose publicly!)
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxxxx
# Publishable key - Safe for client-side (use with RLS)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# Payment Gateway (IPG)
# ============================================================================
# DEV_MODE: When true, skips payment and gives unlimited access
# WARNING: DEV_MODE must be FALSE in production!
# TODO: Integrate a proper IPG (International Payment Gateway) and remove dev mode
DEV_MODE=false
# PayHere integration removed - will be replaced with a new IPG

# ============================================================================
# Application URLs
# ============================================================================
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8080

# ============================================================================
# Google Cloud
# ============================================================================
GOOGLE_CLOUD_PROJECT=seo-pro-production
CLOUD_RUN_LOCATION=us-central1
QUEUE_PATH=projects/seo-pro-production/locations/us-central1/queues/seo-audit-queue

# ============================================================================
# Worker URLs (set after deployment)
# ============================================================================
HTTP_WORKER_URL=https://http-worker-xxxxx.a.run.app
BROWSER_WORKER_URL=https://browser-worker-xxxxx.a.run.app
ORCHESTRATOR_URL=https://orchestrator-xxxxx.a.run.app

# ============================================================================
# Rate Limiting (Optional - enables distributed rate limiting)
# ============================================================================
# Redis URL for distributed rate limiting (optional)
# If not set, falls back to in-memory rate limiting
REDIS_URL=redis://default:password@localhost:6379/0

# ============================================================================
# Environment
# ============================================================================
ENVIRONMENT=development

# ============================================================================
# Admin Configuration
# ============================================================================
# Comma-separated list of admin email addresses for notifications
ADMIN_EMAILS=admin@example.com,another@example.com

# ============================================================================
# SendGrid Email Configuration
# ============================================================================
# Get your API key from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxx
# Email address to send from (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# Display name for sent emails
SENDGRID_FROM_NAME=SEO Pro
